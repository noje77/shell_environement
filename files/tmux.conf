

## <- ADDED BY "INSTALL_SHELL_ENVIRONMENT.SH" REMOVE LINES BELOW IF NEEDED ->

# defaut settings for color, mouse, shell environnement. 
set -g default-terminal "screen-256color"
set-option -g default-shell "/bin/bash"
set -g mouse on

#open window with currrent path                                                                 
bind c new-window -c '#{pane_current_path}'                                                     
#open panel with current path                                                                   
bind '%' split-window -h -c '#{pane_current_path}'                                        
bind '"' split-window -v -c '#{pane_current_path}'  

# Start windows and panes at 1, not 0
set -g base-index 1
setw -g pane-base-index 1

## <- ADDED BY "INSTALL_SHELL_ENVIRONMENT.SH" REMOVE LINES ABOVE IF NEEDED ->


